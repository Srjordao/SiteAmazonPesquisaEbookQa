name: Notificação no Telegram

on:
  workflow_run:
    workflows: ["Testes Regressivos Ebook QA Iniciante"]
    types:
      - completed

jobs:
  sendNotification:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion != 'skipped' }}
    steps:
      - name: Check conclusion status
        id: check_status
        run: echo "::set-output name=conclusion::${{ github.event.workflow_run.conclusion }}"
        
      - name: Send Telegram notification - Success
        if: steps.check_status.outputs.conclusion == 'success'
        run: |
          echo "Workflow: ${{ env.WORKFLOW_NAME }} 💻"
          echo "Evento: ${{ github.event.workflow_run.event }}"
          echo "A esteira foi executada com sucesso ✅"

      - name: Send Telegram notification - Failure
        if: steps.check_status.outputs.conclusion == 'failure'
        run: |
          echo "Workflow: ${{ env.WORKFLOW_NAME }} 💻"
          echo "Evento: ${{ github.event.workflow_run.event }}"
          echo "A esteira falhou ❌"
